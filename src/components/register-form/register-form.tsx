import { component$ } from "@builder.io/qwik";

export const RegisterForm = component$(() => {

  return (
    <div class="wrapper">
      <form action="">
        <input type="text" />
      </form>
    </div>
  )
});